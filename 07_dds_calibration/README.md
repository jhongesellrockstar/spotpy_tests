# Calibración DDS

Ejecute `run_calibration.py`. DDS maximiza directamente NSE y reduce dinámicamente el número de dimensiones perturbadas. Compare sus `metrics.json` con SCE-UA sin inferir superioridad general.

